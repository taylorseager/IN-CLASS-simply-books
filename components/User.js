import React from 'react';
// import { useAuth } from '../utils/context/authContext';

export default function userProfile() {
  const { user } = console.warn('potato');

  return (
    <>
      <div>[{ user }]</div>
    </>
  );
}
